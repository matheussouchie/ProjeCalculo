import { PageShell } from "@/components/app/page-shell";

export default function ProjectsPage() {
  return (
    <PageShell
      title="Projetos"
      description="Lista e acompanhamento dos projetos estimados e concluidos."
    />
  );
}
